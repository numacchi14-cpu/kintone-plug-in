# 変更履歴

## 0.16.1

- 技術用シートの非表示前に帳票シートをアクティブ化し、先頭シートが表示されたままになる不具合を修正した

## 0.16.0

- 完成版テンプレート内の `__PL_FORMULA__:` プレースホルダーを、出力時にExcel数式へ変換するようにした
- 帳票出力ファイルでは、設定・集計・元データ用シートを非表示にし、帳票シートだけを表示するようにした

## 0.15.0

- 基準日を「昨日基準」と「入力基準日」で選んでExcel出力できるようにした
- 一覧画面の選択レコード出力でも、各レコードの基準日と出力履歴を更新できるようにした
- 日付抽出条件の `dateRule` に、日・週・月・年・前年比較の基本パターンを追加した
- テンプレート管理アプリから紐づく帳票出力アプリの任意フィールドを抽出条件に指定できるようにした
- バージョン更新とナンバリングをAIエージェントが判断する運用をSPEC/AGENTSに記載した

## 0.7.0

- 基準日の自動ルールをレコード追加時の初期値として扱い、保存済み・手入力の基準日を出力時に優先するようにした
- 出力アプリの追加・編集画面へ、利用者が明示的に押す `基準日を自動設定` ボタンを追加した
- 日付期間ルールを取得元アプリ単位から各抽出条件の `dateRule` へ移した
- 月初から基準日、1月1日から基準日、基準月全体、前月、翌月などの期間計算に対応した
- 旧 `periodRule` / `additionalQuery` / `valueFrom: "period"` の互換処理を持たず、旧設定を明示的に拒否するようにした
- 簡易生成UI、画面内説明書、README、SPEC、設定例、エージェント引継ぎ文書を更新した

## 0.6.1

- 未稼働の初期開発段階であるため、旧 `dateField` / `storeField` / `sort` の互換処理を削除した
- 抽出条件は `filters`、並び順は `sorts` だけを正式な設定形式にした
- 旧形式を全件取得として誤解釈しないよう、検出時は作り直しを求めるエラーにした
- 型定義、JSON読込、クエリ生成、簡易生成UI、マニュアルから旧形式を削除した

## 0.6.0

- 抽出条件を複数行の `filters` 設定として扱えるようにした
- 条件値に、出力アプリの対象店舗、対象期間、基準日、固定値を指定できるようにした
- 日付フィールドがない取得元アプリを条件なし、または任意条件で取得できるようにした
- 並び順を複数行の `sorts` 設定として扱い、日付条件から独立させた
- 固定値は文字・日付と数値を選べるようにした
- 旧形式の `dateField` / `storeField` / `sort` を引き続き読み込めるようにした
- 日本語フィールドコードをkintoneクエリの正式な記法で出力するようにした
- 簡易生成UI、既定設定、README、SPEC、設定例、画面内説明書を更新した

## 0.5.0

- 取得元アプリIDからフォームのフィールド一覧を取得できるようにした
- kintoneフィールド型からExcel向けの文字列、数値、日付、日時型を自動判定するようにした
- 出力フィールドの全選択、全解除、選択行削除に対応した
- 出力フィールドの上下移動に対応し、Excel列順を画面上で変更できるようにした
- 日付フィールドと店舗フィールドに、取得済みフィールドコードの入力候補を表示するようにした
- テーブル、添付ファイル、関連レコード一覧などの非対応フィールドを自動取り込みから除外し、件数を表示するようにした
- README、SPEC、設定画面内説明書、エージェント引継ぎ文書を更新した

## 0.4.0

- 取得元アプリ設定JSONの簡易生成UIを複数アプリ対応にした
- 取得元アプリブロックの追加、複製、削除に対応した
- 既存JSONから複数の取得元アプリ設定を読み込めるようにした
