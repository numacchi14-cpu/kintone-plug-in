# 変更履歴

## 0.16.11

- v0.16.8で日付ルールへ`baseFirstHalf`／`baseSecondHalf`を追加した際、型定義（`DateRangeRule`）と`calculateDateRange`の実装は更新したが、`src/shared/config.ts`の`parseTemplateSources`が使う実行時の許可リスト（`dateRangeRules`配列）を更新し忘れていた不具合を修正した。この配列に無い`dateRule`値はKintoneに保存されたJSON文字列をパースする際に黙って`sameDay`へ差し替えられてしまうため、正しく`baseFirstHalf`／`baseSecondHalf`を設定していても常に無視され、上半期・下半期出力の対象期間が絞り込まれない（v0.16.10までの修正が効かない）状態になっていた。ユニットテストが対象オブジェクトを直接構築して渡す形だったため、実際のJSON文字列パース経路を通さず今まで発覚しなかった。JSON文字列からのパースを経由する回帰テストを追加した。

## 0.16.10

- 時系列損益計算書の上半期・下半期出力で、対象期間が正しく絞り込まれず（設定シートの対象期間開始・終了が基準日のまま）、PL実績明細が0件で取得される不具合を修正した。原因は`mergeSourceRanges`（複数の取得元アプリの対象期間をまとめて帳票の「対象期間」表示・絞り込みに使う処理）が、日付範囲フィルターを持たないソース（配賦設定履歴など、全件取得するアプリ）を基準日にフォールバックさせたまま他の日付フィルター付きソースと同列に比較していたこと。基準日が対象期間の外にある場合（上半期・下半期出力で基準日が年末寄りのときなど）、フィルターなしソースのダミー値（基準日）が誤って採用されてしまっていた。日付フィルターを持たないソースは対象期間の計算対象から除外するよう修正した。あわせて、これまで`desktop.ts`内にのみ存在し自動テスト対象外だった`sourceDateRange`／`mergeSourceRanges`を`src/shared/kintoneApi.ts`へ移動し、回帰テストを追加した。

## 0.16.9

- 帳票出力時、ExcelJSがワークシートを再シリアライズする際に`<sheetPr>`要素の子要素順序（OOXMLスキーマが要求する`tabColor`→`outlinePr`→`pageSetUpPr`の順）を`pageSetUpPr`→`outlinePr`の順に入れ替えて書き出してしまうことがある不具合を修正した。この順序違反はXMLとしては整形式のため一般的なパーサーでは検出できないが、Excel自身の厳密な読み込み検証では拒否され、シート内容全体（数式・データ）が読み込みエラーとして破棄されてしまう（ファイルが「壊れる」）。テンプレートに元々`sheetPr`（`pageSetUpPr`のfitToPage設定等）があるテンプレートでのみ発生する。出力後に`<sheetPr>`の子要素順序を検査・補正する後処理を追加した。実際に破損していたKintone実出力ファイルに本修正を適用し、Excelで正常に開けて再計算も正しく行われることを確認済み。

## 0.16.8

- 日付ルールへ`baseFirstHalf`（基準日の同年上半期・1月1日～6月30日）・`baseSecondHalf`（基準日の同年下半期・7月1日～12月31日）を追加した。時系列損益計算書を夏季・冬季の賞与査定用に半期単位で出力したいという要望に対応するため。取得元アプリ設定JSONの`dateRule`と、テンプレート編集画面のソース設定プルダウンの両方に追加した

## 0.16.7

- 「選択レコードの基準日を一括設定」を実行した後、Kintone標準の一覧テーブル（基準日列）が更新されず、手動でF5（再読み込み）しないと最新の基準日が表示されない不具合を修正した。原因は、v0.16.6でチェック状態を維持するために画面の再読み込みをやめ、プラグイン独自の選択欄の表示だけをその場で書き換える方式にしていたため、Kintone自身が描画する標準の一覧テーブルには反映されていなかったこと。選択中のレコードIDを一時保存してから画面を再読み込みし、再読み込み後の一覧描画時にチェック状態を復元する方式に変更し、標準の一覧テーブルが最新の基準日を表示しつつ、チェック状態も維持されるようにした（続けて「入力基準日でExcel出力」を押せる）

## 0.16.6

- 帳票出力アプリの一覧画面に「選択レコードの基準日を一括設定」ボタンを追加した。クリックすると日付選択ダイアログが開き、選んだ日付を選択済みレコードの基準日フィールドへ一括で書き込む（Excel出力は行わない）。既存の「昨日基準で出力」「入力基準日で出力」ボタンとは独立した機能で、事前に基準日をまとめて揃えておきたい場合に使う
- ボタンの並びを「昨日基準で出力」「基準日を一括設定」「入力基準日で出力」の順にした（基準日を設定してからすぐ入力基準日で出力する運用を想定）
- 「基準日を一括設定」を実行してもチェック状態が解除されないようにした。従来は成功後に一覧を再読み込みしていたため選択が消えていたが、更新後のメモリ上の値と一覧の表示テキストだけをその場で書き換える方式に変更し、続けて「入力基準日で出力」を押せるようにした

## 0.16.5

- ISO日付文字列（`yyyy-mm-dd`）をExcel日付へ変換する処理を、実行環境のタイムゾーンに依存しないUTC基準・時刻なしの日付として書き込むよう修正した。ローカル時刻で構築していたため、JST環境では日付が時刻付きの端数を持つ値になり、単月帳票の`SUMIFS`完全一致条件が0件になっていた
- 単月テンプレートの右上出力年月日の数式を、`DATEVALUE(LEFT(設定!B7,10))`から`TEXT(設定!B7,...)`の直接参照へ変更した（累計テンプレートは元から直接参照であり変更なし）

## 0.16.4

- 出力日時（`exportedAt`）もExcel日付として設定シートのB7へ書き込むようにした。帳票の和暦表示式が文字列をそのまま返さず、正しく計算されるようにするため

## 0.16.3

- kintoneから取得した`yyyy-mm-dd`形式のISO日付文字列をExcel日付として書き込むようにした。設定JSONが旧来の`text`型のままでも、日付条件付き`SUMIFS`が0件にならないようにするため

## 0.16.2

- 数式プレースホルダーを変換する際に、計算結果`0`をキャッシュ値として保存しないようにした
- 出力ブックの`calcPr`に`calcMode=auto`、`fullCalcOnLoad=1`、`forceFullCalc=1`を設定し、Excelで開いたときに必ず再計算されるようにした

## 0.16.1

- 技術用シートの非表示前に帳票シートをアクティブ化し、先頭シートが表示されたままになる不具合を修正した

## 0.16.0

- 完成版テンプレート内の `__PL_FORMULA__:` プレースホルダーを、出力時にExcel数式へ変換するようにした
- 帳票出力ファイルでは、設定・集計・元データ用シートを非表示にし、帳票シートだけを表示するようにした

## 0.15.0

- 基準日を「昨日基準」と「入力基準日」で選んでExcel出力できるようにした
- 一覧画面の選択レコード出力でも、各レコードの基準日と出力履歴を更新できるようにした
- 日付抽出条件の `dateRule` に、日・週・月・年・前年比較の基本パターンを追加した
- テンプレート管理アプリから紐づく帳票出力アプリの任意フィールドを抽出条件に指定できるようにした
- バージョン更新とナンバリングをAIエージェントが判断する運用をSPEC/AGENTSに記載した

## 0.7.0

- 基準日の自動ルールをレコード追加時の初期値として扱い、保存済み・手入力の基準日を出力時に優先するようにした
- 出力アプリの追加・編集画面へ、利用者が明示的に押す `基準日を自動設定` ボタンを追加した
- 日付期間ルールを取得元アプリ単位から各抽出条件の `dateRule` へ移した
- 月初から基準日、1月1日から基準日、基準月全体、前月、翌月などの期間計算に対応した
- 旧 `periodRule` / `additionalQuery` / `valueFrom: "period"` の互換処理を持たず、旧設定を明示的に拒否するようにした
- 簡易生成UI、画面内説明書、README、SPEC、設定例、エージェント引継ぎ文書を更新した

## 0.6.1

- 未稼働の初期開発段階であるため、旧 `dateField` / `storeField` / `sort` の互換処理を削除した
- 抽出条件は `filters`、並び順は `sorts` だけを正式な設定形式にした
- 旧形式を全件取得として誤解釈しないよう、検出時は作り直しを求めるエラーにした
- 型定義、JSON読込、クエリ生成、簡易生成UI、マニュアルから旧形式を削除した

## 0.6.0

- 抽出条件を複数行の `filters` 設定として扱えるようにした
- 条件値に、出力アプリの対象店舗、対象期間、基準日、固定値を指定できるようにした
- 日付フィールドがない取得元アプリを条件なし、または任意条件で取得できるようにした
- 並び順を複数行の `sorts` 設定として扱い、日付条件から独立させた
- 固定値は文字・日付と数値を選べるようにした
- 旧形式の `dateField` / `storeField` / `sort` を引き続き読み込めるようにした
- 日本語フィールドコードをkintoneクエリの正式な記法で出力するようにした
- 簡易生成UI、既定設定、README、SPEC、設定例、画面内説明書を更新した

## 0.5.0

- 取得元アプリIDからフォームのフィールド一覧を取得できるようにした
- kintoneフィールド型からExcel向けの文字列、数値、日付、日時型を自動判定するようにした
- 出力フィールドの全選択、全解除、選択行削除に対応した
- 出力フィールドの上下移動に対応し、Excel列順を画面上で変更できるようにした
- 日付フィールドと店舗フィールドに、取得済みフィールドコードの入力候補を表示するようにした
- テーブル、添付ファイル、関連レコード一覧などの非対応フィールドを自動取り込みから除外し、件数を表示するようにした
- README、SPEC、設定画面内説明書、エージェント引継ぎ文書を更新した

## 0.4.0

- 取得元アプリ設定JSONの簡易生成UIを複数アプリ対応にした
- 取得元アプリブロックの追加、複製、削除に対応した
- 既存JSONから複数の取得元アプリ設定を読み込めるようにした
