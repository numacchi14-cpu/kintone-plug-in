# SPEC.md

## プロジェクト名

kintone Excel帳票出力プラグイン

## 目的

複数のkintoneアプリからデータを取得し、既存Excelテンプレートの元データ用シートへ書き込んで、見た目重視のExcel帳票を `.xlsx` として出力する。

既存Excelの集計シートにある数式、罫線、書式、レイアウトはExcel側に残す。プラグイン側では集計ロジックを再実装せず、元データ用シートの値更新だけを担当する。

## 基本方針

- 出力形式は `.xlsx`
- PDF出力は対象外
- 既存Excelの見た目維持を優先
- Excelテンプレート方式を採用
- 集計シートの数式はブラウザ側で計算しない
- Excelで開いたときの再計算に任せる
- 必要に応じてブックに再計算設定を入れる
- kintone APIトークン、パスワード、実データは扱わない
- データ取得はログインユーザー権限の `kintone.api()` を使う
- 添付ExcelのダウンロードはkintoneファイルAPIを使う

## 現在の実装範囲

- kintoneプラグイン1本
- プラグイン設定画面で動作モードを切り替え
- テンプレート管理モード
- 帳票出力モード
- 帳票出力レコード詳細画面での出力前チェック
- 帳票出力アプリの一覧画面で、プラグインが表示するチェック欄から選択した複数レコードの一括Excel出力
- 帳票ごとに取得元アプリ設定JSONを保存可能
- テンプレート管理レコード追加/編集画面で複数取得元アプリのJSONを簡易生成可能
- アプリIDからフォームフィールド定義を取得し、型付きで出力フィールドへ取り込み可能
- テンプレート管理アプリと帳票出力アプリを1対1で紐づけ、抽出条件で帳票出力アプリの任意フィールドを参照可能
- 既存設定の再取得時は出力フィールドの構成を維持し、新規フィールドは削除フィールド側へ追加、kintone側になくなったフィールドは通知して除外
- 出力フィールドと削除フィールドの左右表示、削除フィールドからの復元に対応
- 出力フィールドの削除対象選択、削除フィールドへの移動、上下移動に対応
- 出力フィールドと並び順のドラッグ並び替えに対応
- 取得元アプリはJSON設定で複数指定可能
- 取得元ごとの複数抽出条件と複数並び順に対応
- 日付フィールドがない取得元と条件なし全件取得に対応
- 店舗IDなどのキーからマスタアプリを参照する `lookups` に対応
- 初期Excelテンプレートの自動生成
- 完成版Excelテンプレート添付の取得
- 完成版Excelテンプレート添付のアップロード更新補助
- テンプレート管理レコード詳細画面での完成版テンプレート検証
- 設定シートへの出力条件書き込み
- 設定シートへの取得元アプリ別の条件・件数・クエリ書き込み
- 元データ用シートへのkintoneレコード書き込み
- 元データ用シートのExcelテーブル化と行数可変範囲への対応
- 集計シートの保持
- 完成版テンプレートの `__PL_FORMULA__:` プレースホルダーを、出力時にExcel数式へ変換
- 帳票出力ファイルで設定・集計・元データ用シートを非表示にし、帳票シートだけを表示
- 通常は実行日の前日を基準日にし、必要に応じて入力済み基準日でも出力できる
- 日付抽出条件ごとの期間ルールに対応
- 500件超のレコード取得はカーソルAPIで対応

## 対象外

- APIトークン認証
- サーバーサイド処理
- Excel数式のブラウザ側計算
- PDF帳票出力
- 本番実データを使った検証
- kintoneアプリの自動作成

## アプリ構成

最低限、次のkintoneアプリを使う。

1. テンプレート管理アプリ
2. 帳票出力アプリ
3. 年初予算アプリ
4. 日別計画・実績アプリ

同じプラグインをテンプレート管理アプリと帳票出力アプリに適用し、プラグイン設定のモードで動作を切り替える。

## プラグインモード

### テンプレート管理モード

テンプレート管理アプリに適用する。

主な役割:

- 帳票テンプレート情報の管理
- 初期Excelテンプレートの生成
- 使用アプリごとの元データ用シート作成
- 集計シートの空シート作成
- 利用者がExcelで完成版テンプレートを作り込むための土台を出力
- 編集済みExcelを完成版テンプレート添付へ1ファイルで登録する補助
- 完成版テンプレートのシート構成と列見出しを詳細画面で検証する補助

### 帳票出力モード

帳票出力アプリに適用する。

主な役割:

- 帳票種類、対象店舗をレコードから取得
- 基準日は帳票出力時に常に実行日の前日として確定
- 出力前チェックで、テンプレート管理レコード、完成版テンプレート、取得元設定のつながりを確認
- テンプレート管理アプリから完成版Excelテンプレートを取得
- 取得元アプリの日付抽出条件ごとに期間を計算
- 複数のkintoneアプリからデータ取得
- Excelテンプレートの設定シートへ条件を書き込み
- Excelテンプレートの元データ用シートへ値を書き込み
- `.xlsx` としてダウンロード
- 出力時に確定した基準日を帳票出力アプリのレコードへ保存
- 「昨日を基準日にExcel出力」と「入力基準日でExcel出力」を使い分け可能
- レコード一覧画面でプラグインが表示するチェック欄から選択した複数レコードをまとめて出力可能

## kintoneアプリの想定フィールド

### テンプレート管理アプリ

最小構成:

- 帳票ID
- 帳票名
- 完成版テンプレート添付
- 使用アプリ設定
- 使用フィールド設定
- 検索条件設定

現在の実装で使う主なフィールドコードは、プラグイン設定画面で指定する。

- `templateReportIdField`
- `templateReportNameField`
- `templateAttachmentField`
- `templateSourcesJsonField`
- `outputAppId`

初期値:

- 帳票ID: `report_id`
- 帳票名: `report_name`
- 完成版テンプレート添付: `completed_template`
- 取得元アプリ設定JSON: `sources_json`
- 紐づく帳票出力アプリID: `outputAppId`

完成版テンプレート添付は1ファイルだけを正式仕様とする。テンプレート管理アプリの詳細画面に表示される「完成版テンプレート添付を更新」ボタンは、選択した `.xlsx` で添付フィールドを1件に置き換える。

### 帳票出力アプリ

最小構成:

- 帳票種類
- 対象店舗
- 基準日

必要に応じて追加する表示用フィールド:

- 期間開始
- 期間終了
- 出力日時
- 出力者
- 出力ファイル名
- ステータス
- メモ

現在の実装で使う主なフィールドコードは、プラグイン設定画面で指定する。

- `outputReportIdField`
- `outputStoreField`
- `outputBaseDateField`

初期値:

- 帳票種類: `report_type`
- 対象店舗: `store`
- 基準日: `base_date`

## Excelテンプレート仕様

### シート構成

初期生成テンプレートは次のシートを作る。

- `設定`
- 取得元アプリごとの元データ用シート
- `集計`

### 設定シート

出力条件の控えとして使用する。

書き込み項目:

- 帳票ID
- 帳票名
- 対象店舗
- 基準日
- 対象期間開始
- 対象期間終了
- 出力日
- 出力者

既存Excelの集計シートが設定シートを参照している可能性があるため、削除しない。

### 元データ用シート

取得元kintoneアプリごとに1シートを想定する。

プラグインは次を行う。

- 1行目にフィールド見出しを書き込む
- 2行目以降の既存データ行を差し替える
- 取得したkintoneレコードを行として書き込む
- 1行目から取得データ範囲をExcelテーブル化し、行数が変わっても集計シートから構造化参照できるようにする

### 集計シート

完成帳票の見た目を作るシート。

プラグインは原則触らない。

Excel側で次を管理する。

- 数式
- 書式
- 罫線
- レイアウト
- 印刷設定

### 数式プレースホルダーと表示シート

テンプレート作成ツールなどで数式を事前評価できない場合は、セル値を`__PL_FORMULA__:=...`として保存できる。帳票出力時にプラグインがこの接頭辞を除去してExcel数式へ変換し、Excelを開いたときの再計算に委ねる。

出力ファイルでは、`設定`、`集計`、取得元アプリの元データ用シートを非表示にする。利用者に表示するシートは、完成版テンプレートに含めた帳票シートだけとし、その先頭の帳票シートをアクティブにする。帳票シートがない初期テンプレートは、誤って全シートを非表示にしない。

## 取得元アプリ設定JSON

テンプレート管理レコードの `sources_json` に配列として保存する。

プラグイン設定画面の `sourcesJson` は、テンプレート管理レコード側が未入力の場合に使う共通既定値。

利用者向けの詳しい入力例は `SOURCES_JSON_EXAMPLES.md` に分離する。設定画面内マニュアルは冗長化を避け、概要だけを載せる。

テンプレート管理モードのレコード追加/編集画面では、簡易フォームで複数取得元アプリ分のJSONを生成できる。取得元アプリブロックは追加、削除、複製できる。

各取得元アプリブロックでは、アプリIDを指定してフォームフィールド定義APIからフィールド一覧を取得できる。ログインユーザーのセッション認証と権限を使用し、APIトークンは使わない。

「現在のJSONを読み込む」で既存設定を復元した後にフィールド一覧を再取得した場合、既存の出力フィールドの並び順・ラベル・型指定はそのまま維持する。kintone側に新しく増えたフィールドは削除フィールド側に追加し、出力フィールドの構成を自動では変更しない。既存の出力フィールド・削除フィールドのうち、kintone側のフィールド一覧に存在しなくなったものは設定から除外し、除外したフィールド名を通知する。

型変換:

- `NUMBER` / `CALC` / `RECORD_NUMBER`: `number`
- `DATE`: `date`
- `DATETIME` / `CREATED_TIME` / `UPDATED_TIME`: `datetime`
- その他の対応フィールド: `text`

テーブル、添付ファイル、関連レコード一覧、グループなど、現在の1レコード1行形式で正しく出力できないフィールドは自動取り込みから除外し、除外件数を表示する。取り込んだ行は出力フィールドと削除フィールドの左右に分けて管理できる。削除フィールドは後から出力フィールドへ戻せるため、一度すべて削除フィールドへ移動し、必要な順番で戻して列順を整理できる。出力フィールド側ではドラッグまたは上下移動で列順を調整できる。

```json
[
  {
    "key": "annual_budget",
    "label": "年初予算",
    "appId": "123",
    "sheetName": "年初予算",
    "tableName": "tbl_annual_budget",
    "fields": [
      { "code": "store_code", "label": "店舗コード" },
      { "code": "date", "label": "日付", "type": "date" },
      { "code": "budget_amount", "label": "予算金額", "type": "number" }
    ],
    "filters": [
      { "field": "store_code", "operator": "=", "valueFrom": "store", "valueType": "text" },
      { "field": "date", "operator": "between", "valueFrom": "dateRange", "valueType": "text", "dateRule": "yearStartToBaseDate" }
    ],
    "sorts": [
      { "field": "date", "order": "asc" }
    ]
  }
]
```

### 項目

- `key`: 設定上の識別子
- `label`: 取得元アプリの表示名
- `appId`: 取得元kintoneアプリID
- `sheetName`: Excelの書き込み先シート名
- `tableName`: Excelテーブル名。任意。未指定時は `key` や `sheetName` から自動生成
- `fields`: 出力するフィールドコードとExcel見出し
- `filters`: 複数指定できる抽出条件
- `filters[].valueFrom`: `store` / `dateRange` / `baseDate` / `outputField` / `fixed`
- `filters[].outputField`: `outputField` の場合に参照する帳票出力アプリのフィールドコード
- `filters[].valueType`: 固定値などを `text` または `number` としてクエリへ書く指定
- `filters[].dateRule`: `dateRange` の期間計算ルール
- `sorts`: 複数指定できる並び順

抽出条件が0件の場合は、取得元アプリを無条件で取得する。複数の `filters` はANDで連結する。`valueFrom: "outputField"` を使うと、取得元アプリの任意フィールドを、帳票出力アプリの任意フィールド値で絞り込める。

例:

```json
{
  "filters": [
    { "field": "店舗ID", "operator": "=", "valueFrom": "outputField", "outputField": "store_id", "valueType": "text" },
    { "field": "営業部", "operator": "=", "valueFrom": "outputField", "outputField": "department", "valueType": "text" },
    { "field": "日付", "operator": "between", "valueFrom": "dateRange", "valueType": "text", "dateRule": "previousMonthPreviousYear" }
  ]
}
```

正式形式は `filters` / `sorts` とする。旧 `dateField` / `storeField` / `sort` / `periodRule` / `additionalQuery` / `valueFrom: "period"` は互換対応せず、簡易生成UIで設定を作り直す。

### フィールド順と型

Excelへ出力する列順は `fields` 配列の順番をそのまま使う。システム側で勝手に列ソートしない。

元データ用シートは、`tableName` で指定したExcelテーブルとして出力する。集計シートでは `tbl_actual[実績_総売上]` のような構造化参照を使える。`tableName` はExcelの制約に合わせ、英数字とアンダースコア中心の名前を推奨する。未指定の場合はプラグインが `key` や `sheetName` から `tbl_...` 形式の名前を自動生成する。

各フィールドには任意で `type` を指定できる。

対応済み:

- `text`: 文字列
- `number`: 数値
- `date`: 日付
- `datetime`: 日時
- `boolean`: 真偽値

`type` 未指定の場合は、従来どおりkintoneから取得した表示値をそのままExcelへ書き込む。

例:

```json
{ "code": "実績_総売上", "label": "実績_総売上", "type": "number" }
```

`number` 指定の列はカンマを除去して数値へ変換する。変換できない値や空値は空欄にする。

`date` / `datetime` 指定の列はJavaScriptの `Date` としてExcelJSへ渡し、Excel側で日付/日時として扱えるようにする。

## 既存Power Queryから判明した仕様

ユーザー提供の既存Excel Power Queryでは、Excel名前定義から条件値を取得し、kintoneのレコード取得APIで実績データを取得している。

提供されたPower Queryに含まれるsubdomain、appIdはダミー値。APIトークンも実値ではない。実装やドキュメントに認証情報として保存しない。

### Excel側の入力条件

Power Queryでは次のExcel名前定義を参照している。

- `StartDate`: 取得期間開始日
- `EndDate`: 取得期間終了日
- `parlor`: 店舗名
- `KintoneToken`: 既存Excel用のAPIトークン欄。プラグインでは使用しない。

プラグインでは `KintoneToken` は使わず、帳票出力アプリのレコード値とログインユーザー権限の `kintone.api()` で置き換える。

### 既存Power Queryの取得条件

既存Power Queryのkintoneクエリ条件:

```text
日付 >= StartDate
and 日付 <= EndDate
and 店舗名 = parlor
```

プラグイン設定JSONへ落とす場合の対応:

- `filters`: `日付`を基準日から計算する期間、`店舗名`を出力アプリの対象店舗に結び付ける
- 日付フィルターの `dateRule`: 帳票要件に合わせて選択
- 種類や営業部などの追加絞り込みは、固定値のフィルター行として設定

### 既存Power Queryの最終列

既存Power Queryでは、次の列だけを残し、この順番に整形している。

```text
日付
店舗名
営業部
種類
レート
型
実績_営業日数
実績_台数
実績_総IN
実績_総売上
実績_総粗利
実績_総景品
実績_利益率
実績_再プレイ額
実績_補正総売上
実績_補正総粗利
```

この列順は、Excel集計シートが参照している元データテーブルの形として優先的に維持する。
テンプレート管理モードでは、このPower Queryの最終列を「使用フィールド登録」の初期候補として役立てる。最終的な列順は利用者が設定する `fields` 配列順を正とする。

### 数値列

既存Power Queryでは次の列を数値型として扱っている。

```text
レート
実績_営業日数
実績_台数
実績_総IN
実績_総売上
実績_総粗利
実績_総景品
実績_利益率
実績_再プレイ額
実績_補正総売上
実績_補正総粗利
```

プラグイン側ではkintone APIから取得した値をExcelJSへ書き込むため、数値として扱うべきフィールドは `fields[].type = "number"` を指定する。

Power Queryから分かった実績データ用フィールド例:

```json
[
  { "code": "日付", "label": "日付", "type": "date" },
  { "code": "店舗名", "label": "店舗名" },
  { "code": "営業部", "label": "営業部" },
  { "code": "種類", "label": "種類" },
  { "code": "レート", "label": "レート", "type": "number" },
  { "code": "型", "label": "型" },
  { "code": "実績_営業日数", "label": "実績_営業日数", "type": "number" },
  { "code": "実績_台数", "label": "実績_台数", "type": "number" },
  { "code": "実績_総IN", "label": "実績_総IN", "type": "number" },
  { "code": "実績_総売上", "label": "実績_総売上", "type": "number" },
  { "code": "実績_総粗利", "label": "実績_総粗利", "type": "number" },
  { "code": "実績_総景品", "label": "実績_総景品", "type": "number" },
  { "code": "実績_利益率", "label": "実績_利益率", "type": "number" },
  { "code": "実績_再プレイ額", "label": "実績_再プレイ額", "type": "number" },
  { "code": "実績_補正総売上", "label": "実績_補正総売上", "type": "number" },
  { "code": "実績_補正総粗利", "label": "実績_補正総粗利", "type": "number" }
]
```

### 既存Power Queryとの差分

既存Power Query:

- APIトークンを使う
- `/k/v1/records.json` を `limit 500 offset ...` で取得
- 最大10,000件までを想定
- 取得後にkintoneフィールドオブジェクトの `value` を抽出
- 最終列だけに絞り込む
- 数値列と日付列を型変換する

現在のプラグイン:

- APIトークンを使わない
- ログインユーザー権限で `kintone.api()` を使う
- カーソルAPIで500件ずつ取得する
- `value` 相当を抽出してExcelへ書き込む
- 出力列と列順は `sourcesJson.fields` で指定する
- 元データ範囲はExcelテーブルとして出力し、集計シート側はテーブルの構造化参照を使える
- Excel数式は計算せず保持する

## 基準日の決定ルール

前日までの営業実績しか確定しないため、通常の帳票出力では基準日を実行日の前日とする。当月の実績は、昨日までの実績を意味する。

帳票出力アプリのレコード追加画面では、基準日に実行日の前日をセットする。Excel出力時は保存済みの基準日や利用者が手入力した基準日を優先せず、その時点の実行日の前日をリアルタイムに計算して使う。追加・編集画面の `基準日を昨日に更新` ボタンを利用者が押した場合も、実行日の前日をセットする。

正式仕様:

- `yesterday`: 常に昨日にする

既存プラグイン設定に旧ルール値が保存されていても、読み込み時に `yesterday` として扱う。

例:

- 2026-07-30 に開いた場合: 基準日 `2026-07-29`
- 2026-08-01 に開いた場合: 基準日 `2026-07-31`
- 2026-08-03 に、基準日 `2026-07-31` で保存済みのレコードを出力した場合: 基準日 `2026-08-02`

## 日付抽出条件の期間ルール

各取得元アプリの各日付フィルターに `dateRule` を設定する。同じ帳票でも、月次管理アプリと年間管理アプリで異なる範囲を指定できる。

対応済み:

- `sameDay`: 基準日と同じ日
- `previousDay`: 基準日の前日
- `nextDay`: 基準日の翌日
- `baseWeek`: 基準日の週（日曜から土曜）
- `previousWeek`: 基準日の前週（日曜から土曜）
- `nextWeek`: 基準日の翌週（日曜から土曜）
- `baseMonthStart`: 基準日の同月1日
- `baseMonthEnd`: 基準日の同月末
- `monthStartToBaseDate`: 基準日の月初から基準日
- `baseMonth`: 基準日の月初から月末
- `previousMonthStart`: 基準日の前月1日
- `previousMonthEnd`: 基準日の前月末
- `previousMonth`: 基準日の前月
- `nextMonthStart`: 基準日の翌月1日
- `nextMonthEnd`: 基準日の翌月末
- `nextMonth`: 基準日の翌月
- `baseMonthToNextMonthEnd`: 基準日の月初から翌月末
- `baseYearStart`: 基準日の同年1月1日
- `baseYearEnd`: 基準日の同年12月31日
- `yearStartToBaseDate`: 1月1日から基準日
- `baseYear`: 基準日の同年1月1日から12月31日
- `baseFirstHalf`: 基準日の同年上半期（1月1日から6月30日）（v0.16.8で追加。時系列損益計算書の夏季賞与査定用出力に使用）
- `baseSecondHalf`: 基準日の同年下半期（7月1日から12月31日）（v0.16.8で追加。時系列損益計算書の冬季賞与査定用出力に使用）
- `previousYearStart`: 基準日の前年1月1日
- `previousYearEnd`: 基準日の前年12月31日
- `previousYear`: 基準日の前年1月1日から12月31日
- `nextYearStart`: 基準日の翌年1月1日
- `nextYearEnd`: 基準日の翌年12月31日
- `nextYear`: 基準日の翌年1月1日から12月31日
- `sameDayPreviousYear`: 基準日の前年同日
- `sameMonthPreviousYear`: 基準日の前年同月
- `previousMonthPreviousYear`: 基準日の前月の前年同月

kintone標準の日付絞り込みに合わせ、週の範囲は日曜始まり、土曜終わりとする。

例: 基準日が `2026-07-15` の場合

- 月次管理アプリの `monthStartToBaseDate`: `2026-07-01` から `2026-07-15`
- 年間管理アプリの `yearStartToBaseDate`: `2026-01-01` から `2026-07-15`
- 前月前年比較用の `previousMonthPreviousYear`: 基準日が `2026-08-02` の場合、`2025-07-01` から `2025-07-31`

例: `2026-08-01` に基準日が自動で `2026-07-31` になった場合

- 月次管理アプリ: `2026-07-01` から `2026-07-31`
- 年間管理アプリ: `2026-01-01` から `2026-07-31`

## データ取得仕様

取得は `kintone.api()` を使用する。

取得条件:

- `valueFrom: "store"` の条件は、出力アプリの対象店舗で絞り込む
- `valueFrom: "dateRange"` の条件は、基準日と `dateRule` から期間を計算する
- `valueFrom: "baseDate"` の条件は、出力時に確定した基準日、つまり実行日の前日を使う
- `valueFrom: "outputField"` の条件は、帳票出力アプリの `outputField` で指定したフィールド値を使う
- `valueFrom: "fixed"` の条件は、設定された固定値を使う
- `sorts` がある場合、`order by` として付与する

大量取得:

- `/k/v1/records/cursor.json` を使う
- カーソル作成時の `size` は500
- 途中で失敗した場合はカーソル削除を試みる

## テンプレート・設定検証仕様

テンプレート管理モードでは、初期生成したExcelテンプレートを利用者が編集し、完成版テンプレートとして再登録する想定。

利用者編集後に、必要な情報が壊れている、またはkintone設定とズレている可能性があるため、後続実装で検証機能を追加する。

### 検証タイミング

候補:

- テンプレート管理アプリで完成版テンプレートを添付した後
- テンプレート管理アプリの詳細画面で「テンプレート検証」ボタンを押したとき
- 帳票出力アプリの詳細画面で「出力前チェック」ボタンを押したとき
- 帳票出力アプリでExcel出力ボタンを押した直後

帳票出力前に必ず検証する。テンプレート管理モード側では、詳細画面の「テンプレート検証」ボタンから任意実行できる。帳票出力モード側では、詳細画面の「出力前チェック」ボタンから、実データ取得やExcel生成を行わずに連携状態を確認できる。

### kintone設定の検証

検証項目:

- テンプレート管理アプリIDが設定されている
- 帳票IDに一致するテンプレート管理レコードが存在する
- 完成版テンプレート添付が存在する
- 完成版テンプレート添付が1ファイルだけである
- 取得元アプリIDが設定されている
- 取得元アプリが存在し、ログインユーザーに閲覧権限がある
- `outputField` で指定した帳票出力アプリのフィールドが存在する
- `fields[].code` の各フィールドが取得元アプリに存在する
- `filters[].field` が取得元アプリに存在する
- `sorts[].field` が取得元アプリに存在し、並び順に利用できる
- 各フィルターの演算子と値種別が対応する形式になっている

ユーザー向けメッセージ例:

- `テンプレート管理アプリIDが未設定です。`
- `帳票ID「月次実績」のテンプレート設定が見つかりません。`
- `完成版テンプレート添付が見つかりません。`
- `完成版テンプレート添付が複数あります。1つだけ添付してください。`
- `取得元アプリ「実績」のアプリIDが未設定です。`
- `取得元アプリ「実績」（アプリID: 99）が存在しない、または閲覧権限がありません。`
- `取得元アプリ「実績」にフィールド「実績_総売上」が存在しません。`
- `帳票出力アプリにフィールド「store_id」が存在しません。`
- `取得元アプリ「実績」の日付フィールド「日付」が存在しません。`
- `取得元アプリ「実績」の店舗フィールド「店舗名」が存在しません。`
- `取得元アプリ「実績」の抽出条件「種類」が不正です。演算子と固定値を確認してください。`

### Excelテンプレートの検証

検証項目:

- Excelファイルを読み込める
- `設定` シートが存在する
- 各取得元アプリの `sheetName` に対応する元データ用シートが存在する
- 元データ用シートの1行目に、`fields` の列見出しが存在する
- 集計シートが存在する
- 必要に応じて、Excel名前定義やテーブル名が存在する

ユーザー向けメッセージ例:

- `完成版テンプレートExcelを読み込めません。xlsx形式か確認してください。`
- `テンプレートに「設定」シートがありません。`
- `テンプレートに元データ用シート「実績」がありません。`
- `元データ用シート「実績」に列「実績_総売上」がありません。`
- `テンプレートに「集計」シートがありません。`

### 検証結果の扱い

- エラーは一覧でまとめて表示する
- 可能なら「どの帳票」「どの取得元アプリ」「どのシート」「どのフィールド」が問題かを含める
- 警告とエラーを分ける
- エラーがある場合はExcel出力を中断する
- 警告だけの場合は、利用者確認後に続行できる余地を残す

エラー例:

- アプリが存在しない
- フィールドが存在しない
- 完成版テンプレート添付がない
- 完成版テンプレート添付が複数ある
- 必須シートがない

警告例:

- `fields` に設定されている列がテンプレートにないが、出力時に列を自動追加できる
- 集計シートがないが、元データ出力だけなら可能
- 数値型指定のフィールドに数値化できない値が含まれる

## 帳票出力処理フロー

1. 帳票出力アプリの詳細画面にボタンを表示
2. 必要に応じて「出力前チェック」ボタンでテンプレート管理レコード、完成版テンプレート、取得元設定を検証
3. Excel帳票出力ボタンクリック時に帳票種類、対象店舗を取得
4. 「昨日を基準日にExcel出力」では、基準日は保存値ではなく実行日の前日としてリアルタイムに確定する。「入力基準日でExcel出力」では、レコードに入力済みの基準日を使う。
5. テンプレート管理アプリから帳票IDに一致するレコードを取得
6. テンプレート管理レコードの取得元アプリ設定JSONを取得
7. 完成版テンプレート添付が1ファイルだけであることを確認し、`fileKey` を取得
8. 日付抽出条件ごとに基準日から期間を計算
9. 取得元アプリからkintoneレコードを取得
10. 添付Excelテンプレートをダウンロード
11. ExcelブックをExcelJSで読み込む
12. 設定シートへ出力条件を書き込む
13. 元データ用シートへレコードを書き込み、Excelテーブル範囲を更新する
14. 再計算設定を有効化する
15. `.xlsx` としてブラウザからダウンロードする

帳票出力アプリの一覧画面では、プラグインが表示するチェック欄で選択したレコードに対して上記2〜15を1件ずつ順番に実行する。一覧画面でも、昨日基準の出力と入力済み基準日の出力を選べる。1件でも失敗した場合は他のレコードの出力を継続し、完了後に失敗したレコードをまとめてエラー表示する。

## kintone作法

- 画面イベントで `event.record` を変更する場合は `return event;` する
- 保存前イベントで非同期処理を待つ場合はPromiseをreturnする
- 現在の実装では保存前イベント内の非同期処理はない
- kintone標準画面の内部DOM構造には依存しない
- ボタン配置は `kintone.app.record.getHeaderMenuSpaceElement()` を使う
- プラグイン設定値は `kintone.plugin.app.setConfig()` で保存し、実行側で `kintone.plugin.app.getConfig()` から取得する

## 実装ファイル

```text
src/
  desktop.ts
  config.ts
  shared/
    config.ts
    dateRules.ts
    defaults.ts
    excel.ts
    kintoneApi.ts
    types.ts
public/
  config.html
  css/
plugin/
  manifest.json
  image/icon.svg
scripts/
  build.mjs
  build-plugin.mjs
```

## ビルドと検証

PowerShellでは `npm.cmd` を使う。

```powershell
npm.cmd install
npm.cmd run typecheck
npm.cmd test
npm.cmd run package
```

成果物:

```text
dist/kintone-excel-report-plugin-v0.15.0.zip
dist/kintone-excel-report-plugin.zip
```

バージョン付きzipは履歴確認用、バージョンなしzipは最新版用。どちらも同じ内容。

## バージョン運用

バージョン更新とナンバリングは、変更内容に応じてAIエージェントが判断して行う。

原則としてSemVerを目安にする。

- patch: 不具合修正、小さな文言修正
- minor: 後方互換のある機能追加、設定項目追加
- major: 既存設定や利用手順に破壊的変更がある場合

バージョン更新時は、次を揃える。

- `package.json`
- `package-lock.json`
- `plugin/manifest.json`
- `CHANGELOG.md`
- `README.md` / `SPEC.md` 内の成果物名

`npm.cmd run package` で、バージョン付きzipとバージョンなしzipの両方が生成されることを確認する。

既知の警告:

- `kintone-plugin-packer` が `homepage_url.ja` / `homepage_url.en` 未設定の警告を出すことがある
- zip生成が成功していれば現時点では許容

## PL Management 導入・引継ぎ状況（2026-08-12）

この節は、部門別PL再現案件でこのプラグインを個別に継続開発するときの引継ぎ情報である。実運用の接続先、APIトークン、パスワードは記載しない。

### 実装済みの帳票

- 帳票ID: `monthly_department_pl`（単月部門別損益計算書）
- 取得元: `PL実績明細` と `配賦設定履歴` の2アプリ
- 完成版テンプレート: `C:\Projects\PL Management\outputs\monthly-template-20260812-v0.16.5\単月部門別PL_Kintoneテンプレート_完成版_千円表示.xlsx`（v0.16.2版に対して出力年月日の数式のみ変更。詳細は下記「単月の日付不具合とv0.16.5での修正」を参照）
- 取得元アプリ設定JSON: `C:\Projects\PL Management\outputs\monthly-template-20260812\sources_json_単月部門別PL.json`（バージョン間で共通、変更なし）
- 帳票シートは `単月_沖縄`、`単月_九州`。元データ、設定、集計は技術用シートとして扱う。

テンプレートの集計ロジックはExcel数式で実装する。テンプレート作成時に数式を事前評価できないセルは、`__PL_FORMULA__:=...` を入れておき、帳票出力時に通常のExcel数式へ変換する。

金額セルは円のまま計算し、数値書式 `#,###,;[Red]"△ "#,###,` で千円表示・右寄せにする。率は `0.00%` とする。数式プレースホルダーを設定する際、この数値書式を文字列形式へ変更してはならない。

### 取得設定で確認済みの要点

`PL実績明細` は `period_start` を基準日ルールで絞り込み、`aggregation_status` を固定値 `集計対象` で絞り込む。Kintoneのドロップダウン型フィールドには `=` を使えないため、JSONでは必ず次のように `in` を使う。

```json
{ "field": "aggregation_status", "operator": "in", "valueFrom": "fixed", "value": "集計対象", "valueType": "text" }
```

`=` のままにすると、Kintoneが `GAIA_IQ03` を返す。日付・期間・店舗などの具体的な条件は、完成版テンプレートに紐づく取得元アプリ設定JSONを正とする。

テンプレート管理モードと帳票出力モードは、互いの対象アプリIDをプラグイン設定で指定する。アプリIDは環境ごとに異なるため、このリポジトリには固定値を保存しない。

### 検証結果と残作業

- 2025年11月の実Kintoneデータで単月帳票を出力し、旧帳票との全1,518セル照合は差異0件だった。
- v0.16.0の初回出力では、技術用シートのうち先頭シートが表示されたままになる事象があった。v0.16.1で帳票シートをアクティブにしてから技術用シートを非表示にするよう修正済みである。
- v0.16.1を適用後、再出力したファイルで `単月_沖縄` と `単月_九州` だけが表示されることを画面確認する。
- v0.16.2では、数式プレースホルダーを変換する際に計算結果`0`をキャッシュとして保存しない。出力ファイルの`calcPr`へ`calcMode="auto"`、`fullCalcOnLoad="1"`、`forceFullCalc="1"`を設定し、Excelで開いたときに必ず再計算する。累計帳票のA列の縦書き・結合表示はテンプレート（修正版8）側で保持する。
- v0.16.3では、取得元の`yyyy-mm-dd`形式文字列をExcel日付として書き込む。設定JSONが旧来の`text`型であっても期間条件付きの`SUMIFS`が0件にならないよう、ISO形式の日付を自動正規化する。
- v0.16.4では、出力日時（`exportedAt`）もExcel日付として設定シートへ書き込む。帳票の`TEXT(設定!B7, "[$-ja-JP-x-gannen]ggge年m月d日")`が文字列をそのまま返さず、和暦日付を表示できることを確認済み。
- 累計部門別損益計算書は、テンプレート・取得元設定を作成済みである。実Kintone出力で金額、千円表示、A列区分表示、和暦の出力年月日を目視確認した。**ただしこの目視確認は、後日判明した「期間開始月（1月）が範囲条件から漏れる」不具合（下記「単月の日付不具合とv0.16.5での修正」参照）に気づいていない状態で行ったものであり、金額の正しさは保証されない。** 2025年1～11月の旧累計帳票との沖縄・九州の全セル照合は未完了であり、次の受入作業とする。

### 単月の日付不具合とv0.16.5での修正（実装済み・実出力検証も完了）

- 原因は2点の組み合わせだった。(1) `normalizeCellValue`のISO日付正規化が`new Date(year, month - 1, day)`というローカル時刻のコンストラクタを使っており、`dateToExcel`（`d.getTime() / 86400000`、切り捨てなし）で時刻付きの端数を持つExcelシリアル値になっていた。(2) `設定!B5`（`対象期間開始`）はKintoneから取得した`yyyy-mm-dd`のテキストのまま書き込まれ、Excel日付へ変換されていなかった。SUMIFSの等価条件は、テキスト側が日付として解釈できる場合はExcelが暗黙に日付比較へ変換するため、(2)自体は通常問題にならないが、(1)により範囲側（`PL実績明細!D列`）が時刻付きの端数値になっていたため、完全一致が成立しなかった。
- v0.16.5で`normalizeCellValue`を`new Date(Date.UTC(year, month - 1, day))`へ変更し、実行環境のタイムゾーンに関わらず時刻なしの整数シリアル値になるようにした。`exportedAt`（出力日時、`normalizeDateTimeValue`）は`desktop.ts`の`formatDateTime`がローカル時刻の値を文字列化しているため、パース側もローカル時刻のまま維持し、変更していない。
- 単月テンプレートの出力年月日の式（沖縄`M1`/`N1`、九州`G1`/`H1`、いずれもマージセル）を、旧式の`DATEVALUE(LEFT(設定!B7,10))`から、累計テンプレートと同じ直接参照`TEXT(設定!B7,"[$-ja-JP-x-gannen]ggge年m月d日")`へ変更した。`設定!B7`はv0.16.4以降すでにExcel日付値であり、`LEFT()`はセルの表示書式を無視してシリアル値をGeneral書式のテキストへ変換するため、`DATEVALUE(LEFT(...))`はそのままでは正しく動かない状態になっていた。
- 更新後のテンプレートは`C:\Projects\PL Management\outputs\monthly-template-20260812-v0.16.5\単月部門別PL_Kintoneテンプレート_完成版_千円表示.xlsx`。v0.16.2版との差分は上記4セルの数式のみで、他のセル・シート状態に差異がないことをExcelJSで全セル比較して確認済み。取得元アプリ設定JSONは`outputs/monthly-template-20260812/sources_json_単月部門別PL.json`を変更せず流用する。
- `scripts/smoke-test.mjs`に、ISO日付文字列（`'2026-07-01'`）を正規化した結果が`Date.UTC(2026, 6, 1)`と厳密に一致することを確認する回帰テストを追加した（実行マシンはJSTのため、修正前のコードでは失敗していたことを確認済み）。
- 累計帳票は開始日以上・終了日以下の範囲条件で集計する。累計のテンプレート・設定JSON・数式は変更していない。
- **実出力検証（2026-08-12実施）**：ユーザーが実出力した`単月部門別損益計算書_全店舗_2025-11-30 (4).xlsx`（v0.16.5適用後）を、`PL実績明細`の生データから`SUMIFS`と同じ条件をJavaScriptで独立再実装して検証した。沖縄9店舗・九州3店舗×6科目行の57パターンすべてが、1,518セル照合済みの旧帳票の値と完全一致。単月は問題ない。
- **重要な訂正：累計は「金額が変わらない」ではなく「1月分が復活して増える」のが正しい**。当初「開始日以上・終了日以下の範囲条件なので対象月を除外しない」と想定していたが誤りだった。旧バグ（ローカル時刻でのDate構築）は日付を9時間（0.375日）前にずらすため、2月～11月の月初はずれても範囲内に収まるが、**対象期間の開始日（1月1日）だけは前年12月31日15:00になり`>=`条件を割り込み、期間開始月のレコードだけが累計の全SUMIFSから静かに除外されていた**。修正前`(8)`と修正後`(9)`の実出力を突合し、我如古店・具志川店・福岡本店の売上高、期首商品棚卸高（開始月の値を使う特殊行）いずれも差分が1月分の金額と厳密に一致することを確認した（例：我如古店売上高 1,574,939,400円→1,744,278,000円、差額169,338,600円が1月売上高と完全一致）。v0.16.3以降に出力された累計帳票はすべてこの欠落を含んでいた可能性が高い。詳細は`C:\Projects\PL Management\SPEC.md`の「重要な訂正：累計帳票は1月分が欠落していた」を参照。
- 旧累計帳票との沖縄・九州の全セル照合（次の受入作業）はまだ実施していない。今回のように「目視確認」だけでは1月欠落のような集計範囲の誤りは検出できないため、全セル照合を必ず実施すること。

### 累計の全セル照合で判明した追加の不具合（2026-08-12、一部修正済み）

Excel COM自動化（PowerShellから`New-Object -ComObject Excel.Application`）でユーザー実出力`累計部門別損益計算書_全店舗_2025-11-30 (9).xlsx`を実際に開いて`CalculateFullRebuild()`で強制再計算し、`★営業用部門別損益(沖縄)★`（A1:N84）・`★営業用部門別損益(九州)★`（A1:H84）の全セルを、旧帳票の実測値と機械的に突合した（結合セルの非アンカー側はExcelJSが値を複製して見せるだけで実際は空白のため比較対象から除外、旧帳票側の`#DIV/0!`等のエラー値も適切に判定）。比較対象1,756セル中228セルに差異があった。うち日付バグ（1月欠落）に起因するものはなく、**テンプレートの別の不具合**だった。

#### 修正済み：期首・期末商品棚卸高（8行目・11行目）

- 原因：累計テンプレートの8行目（期首商品棚卸高、科目コード`600`）・11行目（期末商品棚卸高、科目コード`614`。部門コード`614`＝ﾙｸｿｰﾙ店と数字が偶然一致しているだけで、これ自体は単月テンプレートでも使われている正しい科目コード）が、他の売上・経費行と同じ「対象期間全体を`>=開始日 かつ <=終了日`で合算するSUMIFS」になっていた。しかし`SPEC.md`の業務要件どおり、期首棚卸高は開始月、期末棚卸高は終了月の**単月の値だけ**を使うべきであり、11ヶ月分を合算してはならない。売上原価・売上総利益の行自体はこの誤りが目立たなかった（期首・期末それぞれの誤差の大きさがたまたま近く、差し引き計算でほぼ相殺されていたため）が、期首・期末棚卸高として**表示される数値自体は誤っていた**。
- 修正：8行目は`'PL実績明細'!$D$2:...,'設定'!$B$5`という単月と同じ完全一致条件に変更（`設定!B5`＝対象期間開始は常に月初日のためそのまま使える）。11行目は`設定!B6`（対象期間終了、例:`2025-11-30`という**月末日**）をそのまま完全一致条件に使うと`period_start`（常に月初日）と一致しないため、`LEFT('設定'!$B$6,8)&"01"`でその月の1日を導出してから完全一致条件に使うよう変更した。
- 検証：修正版を`outputs/cumulative-template-20260812/累計部門別PL_Kintoneテンプレート_完成版_千円表示_修正版9.xlsx`として保存。修正版8との差分は沖縄9店舗・九州3店舗×2行＝24セルの数式のみ（ExcelJSで全セル比較して確認）。数式最長5,911文字で8,192文字上限内。さらに、ユーザー実出力(9)の該当24セルの数式だけをこの新条件に差し替えたコピーを作成し、Excel COMで強制再計算した結果、期首・期末棚卸高が旧帳票の値と全店舗・両地域で完全一致することを確認した。この修正により228件中36件（沖縄24件＋九州12件）の差異が解消され、他の行には影響しないことも確認済み（意図した24セル以外に変化がないことを別途確認）。

#### 修正済み：法人決算ベースPLセクション（53～69行目）の期間計算と会社名ラベル欠落

- 原因：58・59・62・64～68行目（法人決算ベースPLの売上高・売上原価・販管費・営業外収益・営業外費用・特別利益・特別損失）が、店舗別PLと同じ「対象期間（設定!B5～B6、例：2025-01-01～2025-11-30）」で`合計残高試算表`を集計していた。しかし業務要件（`SPEC.md`「下段56～69行：法人決算ベースPL」）どおり、このセクションは「法人マスタの決算月から求めた当期首〜終了月」を使うべきで、対象期間とは異なる期間になりうる。
- 根拠：レガシーシステムのVBソース（`C:\Projects\PL Management\損益管理システム\損益管理システム\Logic400帳票出力.vb`の`Get決算月`関数）を確認したところ、当期首は「決算月が対象終了月以上ならDATE(終了年,決算月,1)、そうでなければDATE(終了年+1,決算月,1)から11ヶ月前」というロジックだった。対象法人（㈱エルスペース、決算月＝3月、`inputs/legacy-master/07_corporations.csv`の法人コード005で確認）と対象終了月2025-11で計算すると、当期首＝2025年4月。ユーザー実出力(9)の生データ（`合計残高試算表`のP1000科目・貸方-借方）を2025年4月～11月（8ヶ月）で独立に再集計したところ、沖縄11,468,677,229円・九州4,573,083,495円となり、**旧帳票の値と完全一致**した（対象期間そのまま集計した2025年1月～11月＝11ヶ月では一致しない）。
- 修正：該当16セル（8行×C/D列×2シート）の日付下限条件を、`設定!B5`から、上記ロジックを移植した`TEXT(EDATE(IF(3>=MONTH(DATEVALUE(設定!B6)),DATE(YEAR(DATEVALUE(設定!B6)),3,1),DATE(YEAR(DATEVALUE(設定!B6))+1,3,1)),-11),"yyyy-mm-dd")`へ変更した。決算月「3」は対象法人に固有の値としてハードコードした（他の科目コード等と同様、テンプレート内に業務定数として直書きする既存の設計方針に合わせた。法人が増えた場合は要見直し）。上限（`<=設定!B6`）は変更していない。
- 併せて、57行目（C57/D57）に会社名ラベル「㈱エルスペース沖縄」「㈱エルスペース九州」が空白になっていた（旧帳票には存在）ため、固定文字列として追加した。
- 検証：修正版を`outputs/cumulative-template-20260812/累計部門別PL_Kintoneテンプレート_完成版_千円表示_修正版10.xlsx`として保存。修正版9との差分は36セル（意図した16数式セル×2列×2シート＋ラベル2セル×2シート）のみ。ユーザー実出力(9)の該当セルへ同じ修正を適用したコピーをExcel COMで強制再計算し、法人決算ベースPLの売上高・売上原価等が旧帳票と完全一致することを確認した。この修正で154セルへ減少。

#### 修正済み：間接費共通プールの税区分誤り（48・50・51行目）

- 原因：`集計`シートの月別按分マトリクス（行66「間接費共通プール oki」、行91「間接費共通プール kyushu」、各12列＝月別）が、非生産部門（沖縄`949`＝㈱ｴﾙｽﾍﾟｰｽ沖縄支店共通、九州`999`＝同九州支店共通）の`P5000`（経常利益）を**税抜**で取得していた。単月帳票の検証済み数式は**税込**を直接使う（`SPEC.md`「これは税込`P5000`の符号反転に等しい」）。税抜だけでは消費税相当分が抜け落ち、配賦プールの絶対値が小さくなり、間接費配賦額・総経費・店舗最終利益が一貫して過小（約1.8%低い）になっていた。
- 根拠：2025年11月・部門949・P5000で実測すると、税抜`-28,796,720円`、税込`-29,369,076円`。旧帳票の2025年11月間接費配賦額（沖縄合計`29,369,076円`、`SPEC.md`本文に既出）と税込側が完全一致する。
- 修正：`集計!B66:M66`・`集計!B91:M91`（各12セル、計24セル）の税区分条件を`"税抜"`から`"税込"`へ変更した。
- 検証：ユーザー実出力(9)の該当セルを同じ条件へ差し替えたコピーをExcel COMで再計算し、48・50・51行目が旧帳票と全店舗・両地域で完全一致することを確認した（前段の期首・期末棚卸高修正と合わせて154件→96件へ減少）。

#### 修正済み：法人決算ベースPLのE・F列余剰セル（58～69行目）

- 原因：58～69行目のE列・F列（会社名ラベルのないセル）に`=SUM(C:D)`とその複製という「合計」数式が残っていた。旧帳票ではこの範囲は完全に空白であり、このセクションはC列（沖縄）・D列（九州）の2列だけで完結する。店舗別PLのC～N列パターンをテンプレート構築時にそのままコピーした際の余剰セルだった。
- 修正：該当48セル（12行×E/F列×2シート）を空白へ戻した。
- 検証：Excel COMでの再計算により96件→48件へ減少。

#### 修正済み：比較手法自体の誤検出（6・14・30・40・82行目）

全セル照合の当初の実装は、旧帳票側をExcelJSでの静的読み取り（キャッシュされた計算結果）、新出力側をExcel COMでの強制再計算後の実測値、という**異なる方法で取得した値同士**を比較していた。これが原因で、結合セルの値複製やキャッシュ欠落のある共有数式セル（例：82行目E/F、旧帳票側は実際には0だがキャッシュ値が存在せず比較上「エラー扱い」になっていた）で、実在しない差異を誤検出していた。

旧帳票側もExcel COMで強制再計算した実測値に統一して比較し直した結果、6・14・30・40・82行目の差異は**すべて誤検出**で、実際には最初から一致していたことが判明した。以後の全セル照合は、比較対象双方を同じ方法（実際にExcelで開いて再計算した値）で取得すること。

#### 修正済み：配賦基準台数（パチンコ・スロット台数）の基準日誤り（53・54行目）

- 原因：53・54行目（沖縄`L53`/`L54`、九州`F53`/`F54`、参考表示の配賦基準台数合計）が、`配賦設定履歴`の`有効開始日`を`設定!B5`（対象期間開始、2025-01-01）と完全一致させていた。しかし九州の福岡本店・福岡東店は年度途中（2025年4月・10月）に台数変更があり、期首時点の台数と実際の対象期間終了（基準日）時点の台数が異なっていた。
- 根拠：`inputs/legacy-master/02_department_attributes_2025_2026.csv`で確認したところ、2025年11月時点の九州3店舗合計はパチンコ`468+168+240=876`台・スロット`446+143+312=901`台で、旧帳票の値と完全一致した（1月時点の台数では合計`992`台・`787`台になり、修正前の誤った出力と一致する）。
- 修正：該当4セル（沖縄`L53`/`L54`、九州`F53`/`F54`、各セル内の複数店舗分SUMIFSすべて）の日付条件を`設定!B5`から`LEFT(設定!$B$6,8)&"01"`（対象期間終了月の1日）へ変更した。沖縄側は期間中に台数変更がなかったため数値上は変化しないが、将来の期間変更に備えて同じロジックへ統一した。
- 併せて、K53/K54（沖縄）・E53/E54（九州）に「P:」「S:」という参考ラベルの静的文字列が抜けていたため追加した。
- 検証：Excel COMでの再計算により、48件→0件（出力年月日セルを除く）。

### 累計の全セル照合：最終結果（0件差異、修正版14、実出力で確認済み）

6種類の不具合修正（期首・期末棚卸高、法人決算ベースPLの期間計算、会社名ラベル、間接費共通プールの税区分、E/F余剰セル、配賦基準台数の基準日）を経て、`outputs/cumulative-template-20260812/累計部門別PL_Kintoneテンプレート_完成版_千円表示_修正版14.xlsx`は、ユーザー実出力(9)へ同じ修正一式を適用したコピーをExcel COMで強制再計算した結果、**旧帳票（Excel COMで同様に再計算した実測値）と1,848セル中1,846セルが完全一致**した（残り2セルは出力年月日で、実出力の日時が異なるのは仕様どおり）。数式最長5,911文字で8,192文字上限内。

**2026-08-12、実Kintone環境での最終確認も完了**：修正版14をテンプレート管理アプリ（179）の`cumulative_department_pl`レコードへ実際に反映し、出力用アプリ（180）で2025年1月～11月累計を再出力した。1回目(10)は誤って単月テンプレートが添付されたままだったため単月形式の出力になっていたが（`設定`シートの対象期間は累計のまま2025-01-01～2025-11-30だったため、単月テンプレートの完全一致条件がたまたま1月分だけを拾って表示していた。タイトルが「単月損益計算書」になっていたことと行数69行で発覚）、テンプレート添付を修正版14へ差し替えて再出力した(11)は、Excel COMでの強制再計算により**旧帳票と1,848セル中1,846セルが完全一致**した（残り2セルは出力年月日、仕様どおり）。累計帳票は受入完了である。

これらは今回の日付バグ修正とは無関係な、テンプレート側の既存の問題だったが、すべて解消し、実出力での検証も完了した。

この案件の業務要件・Kintoneアプリ構成・受入基準は、親プロジェクトの `C:\Projects\PL Management\SPEC.md` を正とする。本リポジトリでは、プラグインの実装、設定JSONの互換性、Excel出力の検証手順を管理する。

### Claude Codeによる精査（2026-08-12）

- `npm.cmd run typecheck` / `npm.cmd test` は現行ブランチで成功、作業ツリーもクリーンであることを確認した。
- 単月の日付不具合（上記「単月の日付不具合と次の修正」）はコード（`src/shared/excel.ts`の`normalizeCellValue`/`normalizeDateTimeValue`）を直接確認し、記載どおり未修正であることを確認した。
- **解消済み（2026-08-13確認）**: 直近のv0.16.x一連の作業（2026-08-01以降）は当時ローカルブランチ`agent/documentation-status`上に未プッシュで残っていたが、その後のセッションで`origin/agent/documentation-status`へプッシュ済みとなり、`main`ブランチも`agent/documentation-status`と同一コミット（`0801a0c`）まで進んでいることを確認した。ローカル`main`・`origin/main`・`agent/documentation-status`（ローカル・リモートとも）はすべて同じコミットを指しており、作業内容の消失リスクはない。working treeもクリーン。

### 単月・累計テンプレートのA1タイトル不具合修正（2026-08-13、テンプレート直接編集・プラグイン改修なし）

ユーザーが基準日2025-12-30で単月・累計を出力したところ、両テンプレートともA1（沖縄・九州シート）のタイトルが「令和7年11月」（累計は「令和7年1月～令和7年11月」）のまま固定されており、対象月を変えても表示が変わらない不具合が発覚した。

- 原因：A1は2025年11月検証時点の固定文字列（共有文字列）のままテンプレートへ埋め込まれており、M1（出力年月日）とは異なり数式化されていなかった。
- 修正：M1と同じ考え方（`DATEVALUE`＋和暦`TEXT`書式）でIF数式化した。
  - 単月：`IF('設定'!$B$5="","",TEXT(DATEVALUE('設定'!$B$5),"[$-ja-JP-x-gannen]ggge年m月")&"　単月損益計算書【地域】")`
  - 累計：`IF('設定'!$B$5="","",TEXT(DATEVALUE('設定'!$B$5),"[$-ja-JP-x-gannen]ggge年m月")&"～"&TEXT(DATEVALUE('設定'!$B$6),"[$-ja-JP-x-gannen]ggge年m月")&"　累計損益計算書【地域】")`
  - `設定!B5`・`B6`（対象期間開始・終了）はISO文字列（`t="s"`）のまま書き込まれているため、`DATEVALUE`で日付化してから`TEXT`の和暦書式へ渡す。空欄時（テンプレート単体で開いた場合）にエラーにならないようIFで空文字を返すガードを入れた。
- 検証：Python（zipfile）でセル内容をシート内`<c r="A1">`の直接置換により数式化し、Excel COM（`CalculateFullRebuild()`）で実際に計算させて確認した。2025年12月分出力の再現テストで「令和7年12月」（累計は「～令和7年12月」）と正しく表示され、両テンプレートの`★営業用部門別損益`シート全セルをスキャンして`#`から始まるエラー値が0件であることも確認した。テンプレート単体（`設定`シート未入力）で開いてもA1は空文字になりエラーにならないことも確認した。
- **この修正はテンプレートファイル（`.xlsx`）の直接編集であり、プラグインのTypeScriptコード・バージョンは変更していない**。修正前のファイルは同フォルダに`.bak-before-a1fix`として保持している。

### 2025年12月分の全セル照合と累計テンプレートの追加修正（2026-08-13）

ユーザー提供の2025年12月分実出力4ファイル（単月・累計のKintone出力、旧システム実出力`.xls`2本）をExcel COMで強制再計算して全セル照合した。**単月・累計とも出力年月日を除いて完全一致**し、上記A1修正版で12月分も正しく計算されることを確認した。照合中、`Range.Value2`を大きな範囲（例：A1:N69）で一括読み取りすると行1付近の結合セルの影響で値がずれて返ることを発見した。今後同様の照合を行う場合は1行ずつ読み取ること。

あわせて、累計テンプレートに次の2点を追加修正した（COMで旧帳票の実際の設定値を確認したうえで適用、詳細は`C:\Projects\PL Management\SPEC.md`の同名見出しを参照）。

1. 印刷範囲：累計テンプレートのみ未設定だった。沖縄`$A$1:$O$86`、九州`$A$1:$I$86`を設定（単月テンプレートは既に旧帳票と一致・変更なし）。
2. ウィンドウ枠固定：累計テンプレートには未設定だった。`SplitRow=3`・`SplitColumn=2`（C4基準）を設定。

修正はExcel COMで直接プロパティ設定→保存する方式で行った。保存後、印刷範囲・ウィンドウ枠固定・A1数式・`__PL_FORMULA__`プレースホルダー（1,554件）・エラーセル0件を再確認済み。修正前のファイルは`.bak-before-printarea-freeze`として保持。この修正もテンプレートファイルの直接編集であり、プラグインのコード・バージョンは変更していない。

**訂正（同日）**：当初「単月テンプレートは列のみ固定で旧帳票と一致・変更なし」としたが、ユーザーから訂正があった。単月の旧帳票には行方向のウィンドウ枠固定が元々無く、今回が初めての追加要望である。単月テンプレートも1～3行目が見出し・4行目からデータという同じ構造のため、累計と同じC4基準（`SplitRow=3`・`SplitColumn=2`）を単月テンプレートの沖縄・九州シートにも追加した。既に列のみの固定（`SplitColumn=2`）が設定済みだったため、`FreezePanes=True`を再設定するだけでは選択位置が反映されず、一度`FreezePanes=False`で解除してからC4を選択し直す必要があった（Excel COM利用時の申し送り事項）。修正後、印刷範囲（変更なし）・A1数式・エラーセル0件を再確認済み。修正前のファイルは`.bak-before-freeze`として保持。

- **2026-08-13、累計はA1タイトル修正・印刷範囲・ウィンドウ枠固定の3点、単月はA1タイトル修正・ウィンドウ枠固定の2点をまとめて反映した修正済みテンプレートを、Kintoneのテンプレート管理アプリ（179）へ再アップロード済み。ユーザーが実機で反映を確認した。**

### 帳票出力アプリ一覧画面への「基準日を一括設定」ボタン追加（v0.16.6、2026-08-13）

ユーザーから、出力用アプリ（180）の一覧画面にある「選択レコードを昨日基準でExcel出力」「選択レコードを入力基準日でExcel出力」に加えて、カレンダーで選んだ日付を選択レコードの基準日へまとめて入力できるボタンが欲しいという要望があった。

- `src/desktop.ts`の`app.record.index.show`ハンドラに3つ目のボタン「選択レコードの基準日を一括設定」を追加した。クリックすると`showBaseDateDialog`が日付選択ダイアログ（`<dialog>`＋`<input type="date">`、ネイティブのカレンダーUIを利用）を表示し、選んだ日付を`bulkUpdateBaseDate`が選択レコードの`outputBaseDateField`（既定`base_date`）へ100件ずつ`kintone.api('/k/v1/records.json', 'PUT', ...)`で一括書き込みする。Excel出力は行わない（あくまで基準日の下準備用）。
- レコード選択は既存の`indexRecordPickerSelection`（チェック欄）をそのまま使う。「全レコード」ではなく「選択したレコード」に対して動作する点は既存2ボタンと同じ挙動に揃えた（`全選択`ボタンを併用すれば実質全件にも適用できる）。
- `showModal`未対応環境（フォールバック）では`window.prompt`で日付文字列を受け取る。
- CSSは`public/css/desktop.css`に`.krp-date-dialog`系を追加（`.krp-error-dialog`と同系統のスタイル）。`npm run package`実行時に`plugin-package/css/`へ自動コピーされる。
- ボタンの並び順を「昨日基準で出力」「基準日を一括設定」「入力基準日で出力」に変更した（基準日を設定した直後に入力基準日で出力する運用を想定）。また「基準日を一括設定」の実行後に一覧を`location.reload()`していたためチェック状態が消えていた不具合を修正し、更新後の値をメモリ上のレコードと一覧表示テキストへその場で反映する方式（`refreshRecordPickerBaseDates`）へ変更した。これにより「基準日を一括設定」→続けて「入力基準日で出力」という操作がチェックを付け直さずに行える。
- `npm.cmd run typecheck` / `npm.cmd run test` / `npm.cmd run build` / `npm.cmd run package`をすべて実行し成功を確認済み。配布ZIPは`dist/kintone-excel-report-plugin-v0.16.6.zip`。
- **2026-08-13、Kintoneのプラグイン管理画面でv0.16.6へ更新し、出力用アプリ（180）で実機動作確認まで完了した。** 「基準日を一括設定」ボタンから日付選択→複数レコードの基準日が更新されること、チェック状態が維持されることをユーザーが実際に確認済み。

### 「基準日を一括設定」実行後に一覧テーブルが更新されない不具合の修正（v0.16.7、2026-08-14）

ユーザーから、「選択レコードの基準日を一括設定」を実行しても、Kintone標準の一覧テーブル（基準日列）の表示が更新されず、手動でF5（再読み込み）しないと最新の基準日が見えないという不具合報告があった。

- 原因：v0.16.6でチェック状態を維持するために画面の再読み込みをやめ、`refreshRecordPickerBaseDates`でプラグイン独自の選択欄（チェックボックスのラベルテキスト）だけをその場で書き換える方式にしていた。この方式は、プラグインが自前で描画する選択欄の表示は更新するが、**Kintone自身が描画する標準の一覧テーブル（`app.record.index.show`が提供する`event.records`を元にKintoneが内部で描画するグリッド）には触れない**ため、そちらは古い値のまま残っていた。標準画面の内部DOM構造には依存しない方針（本ファイル「kintone作法」参照）のため、標準テーブルのセルを直接書き換える対応は避けた。
- 修正：`refreshRecordPickerBaseDates`を廃止し、代わりに選択中のレコードIDを`sessionStorage`（キー`krp-index-selection-<appId>`）へ一時保存してから`reloadPageSoon()`で画面を再読み込みする方式（`saveSelectionForReload`）に変更した。再読み込み後、`app.record.index.show`が再度発火して一覧を描画する際、`renderIndexRecordPicker`の末尾で`restoreSelectionAfterReload`を呼び、保存しておいたIDと一致するチェックボックスへチェックを入れ直す（同時に`sessionStorage`のエントリは消費して1回限りにする）。これにより、Kintone標準の一覧テーブルは実際のページ遷移で最新データを取得・描画するため基準日が正しく更新され、かつプラグイン側のチェック状態も復元されるため、続けて「入力基準日でExcel出力」を押せる従来の利便性も維持される。
- `npm.cmd run typecheck` / `npm.cmd run test` / `npm.cmd run package`をすべて実行し成功を確認済み。配布ZIPは`dist/kintone-excel-report-plugin-v0.16.7.zip`。
- **2026-08-15、ユーザーが実機（ブラウザ）での動作確認・Kintoneプラグイン管理画面への反映を完了**。出力用アプリ（180）で「基準日を一括設定」→一覧テーブルの基準日が即座に更新されること・チェック状態が維持されることの両方を確認済み。

## 帳票出力ファイルが開けなくなる重大な不具合の調査と修正（v0.16.9、2026-08-15）

ユーザーが単月部門別損益計算書（空港店版、`monthly_department_pl_2026`）を実出力したところ、Excelで開こうとすると「読み取れなかった内容を修復または削除することにより、ファイルを開くことができました」という修復ダイアログが出て、`/xl/worksheets/sheet5.xml`・`/xl/worksheets/sheet6.xml`（沖縄・九州の帳票シート）の内容が丸ごと失われる不具合が発生した。同じ空港店版の累計・時系列は正常に出力できており、単月だけで発生した。

### 調査の経緯

- ZIP構造・全XMLパーツの整形式・テーブル定義・スタイル数・共有文字列の整合性・数式の長さ（8,192文字上限）・括弧/引用符バランス・行/セルの並び順など、機械的に検証できる項目はすべて正常だった。
- Excel COM自動化（`Workbooks.Open`）は一貫して失敗し、`DisplayAlerts=$false`の自動化コンテキストでは修復ダイアログの応答待ちで失敗しているだけと分かった（ユーザーの手動ダブルクリックでは修復ダイアログが出て開けた）。
- 「正常な2025年版（福岡東店）テンプレートの出力ファイル」を対照として、ワークブックの部品（シート・スタイル・共有文字列・テーマ・docProps・ワークブック本体）を1つずつ入れ替える二分探索的な比較を数十回実施し、問題が`sheet5.xml`／`sheet6.xml`自体に閉じていることを特定した。
- さらにテンプレートファイル自体（出力前の完成版テンプレート）はExcelで正常に開けることを確認済みだったため、**テンプレートではなく出力処理（`fillReportTemplate`）が問題を作り込んでいる**と判断し、テンプレートの`sheet5.xml`と壊れた出力の`sheet5.xml`の全要素（`sheetPr`・`sheetViews`・`sheetFormatPr`・`cols`・`mergeCells`・`pageMargins`・`pageSetup`）を1つずつ機械比較した。

### 真因

`<sheetPr>`要素の子要素順序が、テンプレートでは`<outlinePr/><pageSetUpPr/>`（OOXMLスキーマCT_SheetPrが要求する`tabColor, outlinePr, pageSetUpPr`の順で正しい）だったのに対し、出力後は`<pageSetUpPr/><outlinePr/>`と入れ替わっていた。この順序違反はXMLとしては整形式（`.NET`や一般的なXMLパーサーはエラーにしない）だが、Excel自身の厳密な読み込み検証では拒否され、該当シートの内容が丸ごと読み込みエラーとして破棄される。

`kintone-plug-in`自身のソースコードには`pageSetup`・`sheetPr`・`outlinePr`を直接操作する箇所が存在しないため、これはExcelJSがワークシートを読み込み→変更→書き出しする過程で内部的に`<sheetPr>`を再構築する際に生じる、ExcelJS側の挙動（原因・再現条件は特定できていない）と考えられる。テンプレートに元々`sheetPr`（`outlinePr`・`pageSetUpPr`）がある帳票（単月部門別損益計算書）でのみ発生し、累計・時系列では発生しなかった（テンプレート側の`sheetPr`構成の違いによるとみられる）。

### 修正

`src/shared/excel.ts`に`fixSheetPrElementOrder`関数を追加した。`fillReportTemplate`が`workbook.xlsx.writeBuffer()`で生成したZIPを、既存の`ensureAutomaticRecalculation`と同じ「JSZipで読み込み→対象パーツを文字列として正規表現で検査・補正→再書き込み」という後処理パターンで実装している。全`xl/worksheets/sheetN.xml`パーツについて、`<sheetPr>`内の`tabColor`・`outlinePr`・`pageSetUpPr`（いずれも存在する場合のみ）を正しい順序へ並べ替える。既に正しい順序の場合は変更しない。

### 検証

- 単体テスト（`scripts/smoke-test.mjs`）に、順序が入れ替わった`sheetPr`を含む合成ワークシートで正しく補正されること、および既に正しい順序の場合は変更しないことの2パターンを追加した。
- **実際に破損していたユーザー提供の実出力ファイルへ本修正を直接適用し、Excel COMで正常に開けること、`CalculateFullRebuild()`による強制再計算で数式が正しく評価されることを確認した**（机上のテストだけでなく、実際に発生した破損インスタンスでの修正確認）。

## 時系列の上半期・下半期出力で対象期間が絞り込まれない不具合（v0.16.10、2026-08-15）

v0.16.8で時系列損益計算書の上半期・下半期出力に対応した後、ユーザーが取得元アプリ設定JSONの`dateRule`を正しく`baseFirstHalf`／`baseSecondHalf`に設定して出力したにもかかわらず、設定シートの「対象期間開始」「対象期間終了」（B5/B6）が両方とも基準日のまま変わらず、PL実績明細が0件で取得される（＝データが何も表示されない）不具合が発生した。

### 原因

`mergeSourceRanges`（`src/shared/kintoneApi.ts`、旧`desktop.ts`）は、帳票が使う複数の取得元アプリ（この帳票の場合はPL実績明細と配賦設定履歴）それぞれの対象期間を集約し、全体の「対象期間開始」＝各ソースの開始日の最小値、「対象期間終了」＝各ソースの終了日の最大値として設定シートへ書き込む。

配賦設定履歴は全件取得するアプリで日付範囲フィルターを持たない。この場合、旧`sourceDateRange`は「日付フィルターがないので期間の概念がない」ことを表現するつもりで基準日（今回は2025-12-15）にフォールバックしていたが、これが**日付フィルター付きソースの本当の期間と同列に扱われて最小・最大の計算に混入していた**。上半期出力（PL実績明細の対象期間：2025-01-01～2025-06-30）の場合、配賦設定履歴側のフォールバック値2025-12-15が「終了日の最大値」として採用されてしまい、対象期間終了が誤って2025-12-15になっていた。

この不具合は基準日が対象期間の外にある場合（通年出力なら基準日は常に対象期間内なので問題にならないが、上半期・下半期のように基準日が対象期間の外にあり得る場合）にのみ顕在化する。累計・単月では発生しなかった理由もこれで説明がつく。

### 修正

`sourceDateRange`が、日付範囲フィルターを持たないソースについては基準日ではなく**空文字列**を返すよう変更した。`mergeSourceRanges`側の`filter(Boolean)`により、空文字列のソースは最小・最大の計算対象から自動的に除外される（既存のフィルター処理を変更する必要はなかった）。

あわせて、この2つの関数はこれまで`desktop.ts`内にのみ定義されており自動テストの対象外だったため、`src/shared/kintoneApi.ts`（自動テスト済みの共有モジュール）へ移動し、回帰テスト（日付フィルターありソースの期間、日付フィルターなしソースが空文字列を返すこと、基準日が対象期間の外でも正しい期間が採用されること、全ソースが日付フィルターなしの場合は基準日にフォールバックすること）を追加した。

## 上半期・下半期のdateRuleが実際には一度も適用されていなかった不具合（v0.16.11、2026-08-15）

v0.16.10をKintoneへ反映し、ユーザーが取得元アプリ設定JSONの`dateRule`を正しく`baseFirstHalf`／`baseSecondHalf`へ設定し直して再出力しても、設定シートの対象期間が絞り込まれない状態が続いた。ブラウザのハード再読み込み（Ctrl+F5、タブの閉じ直し）を行っても症状は変わらなかった。

### 原因

v0.16.8で日付ルールへ`baseFirstHalf`／`baseSecondHalf`を追加した際、以下の3箇所を更新する必要があったが、3つ目を見落としていた。

1. `src/shared/types.ts`の`DateRangeRule`型定義（更新済み）
2. `src/shared/dateRules.ts`の`calculateDateRange`実装（更新済み）
3. `src/shared/config.ts`の`parseTemplateSources`が使う実行時の許可リスト`dateRangeRules`配列（**更新し忘れていた**）

`parseTemplateSources`は、Kintoneのテンプレート管理レコードに保存された取得元アプリ設定JSON（文字列）を`JSON.parse`した後、各フィルターの`dateRule`を`dateRangeRules.includes(...)`で検証し、リストに無い値は問答無用で`'sameDay'`へ差し替える（`normalizeSources`内、`dateRule: dateRangeRules.includes(...) ? ... : 'sameDay'`）。`baseFirstHalf`／`baseSecondHalf`はこの配列に無かったため、Kintone側でどれだけ正しく設定しても、実行時には常に`'sameDay'`（基準日当日のみの範囲）へ差し替えられていた。

この結果、v0.16.8〜v0.16.10でこれまで行った上半期・下半期関連の修正（日付ルール自体の追加、`mergeSourceRanges`の不具合修正）は、いずれもコードとしては正しかったにもかかわらず、**この許可リスト漏れによって一度も実際には適用されていなかった**。

### なぜ今まで発覚しなかったか

既存のユニットテスト（`calculateDateRange('baseFirstHalf', ...)`や`sourceDateRange(source('baseFirstHalf'), ...)`）は、テストコード内でオブジェクトを直接構築して関数へ渡していたため、`parseTemplateSources`のJSON文字列パース経路を一度も通っていなかった。型チェック（`tsc`）も、`dateRangeRules`が単なる`string[]`実行時配列であるため検出できない。

### 修正

`dateRangeRules`配列へ`'baseFirstHalf'`・`'baseSecondHalf'`を追加した。あわせて、`parseTemplateSources`を実際にJSON文字列としてパースする回帰テストを追加し（`scripts/smoke-test.mjs`に`config.ts`のビルド・テストを新設）、今後同様の追加漏れがあれば検出できるようにした。

### 教訓

新しい列挙値・許可される値を追加する際は、**型定義だけでなく、それを実行時に検証する全ての箇所**（`grep`等で同種のパターンを横断的に確認する）を更新すること。ユニットテストは、実際の入力経路（この場合はJSON文字列のパース）を経由する形で書くこと。オブジェクトを直接構築して渡すテストは、パース・検証ロジック自体のバグを検出できない。

## 既知の制約

- ExcelJSはExcel数式を計算しない
- Excelで開いたときに再計算される想定
- 環境によってはF9再計算が必要になる可能性がある
- テンプレート管理アプリ上で完成版テンプレート添付を1ファイルに置き換える保存補助は実装済み
- テンプレート・設定検証機能は帳票出力前のExcelブック基礎検証まで実装済み
- 出力履歴保存は任意フィールド指定時のみ実装済み
- エラー表示は帳票出力時の詳細ダイアログまで実装済み。一部の設定補助UIは簡易alertを継続
- モバイル画面は対象外

## 解消済みの懸案事項

コードレビューで判明した優先度の高い事項は、現時点で次の対応を入れている。

- 取得元アプリ設定JSONの読み込み失敗時は `console.warn` を出し、無言で既定値へ差し替わらないようにした。
- 設定シートの行定義を `src/shared/excel.ts` 内の共通定義に集約し、初期生成と出力時書き込みで共有するようにした。
- 帳票出力前にExcelテンプレートを検証し、`設定`、`集計`、元データ用シート、列見出しが不足している場合は出力を中断するようにした。
- `writeSourceSheet` は、`sheetName` に一致するシートがない場合に新規作成せず、エラーにするようにした。
- `excel.ts` のExcelテーブル生成・範囲更新はスモークテストで検証するようにした。
- `writeSourceSheet` / `writeSettingsSheet` で使っていたExcelJSの `spliceRows` は、削除範囲がシート末尾まで達する呼び出しだと実際には削除しないことを確認した。今回の取得件数・取得元アプリ数が前回より少ない場合に古い行が残る不具合があったため、新データ書き込み後に残った行を明示的に空値化する `clearLeftoverRows` に置き換えて修正した。
- `writeSourceSheet` で、セルへの直接書き込みとExcelテーブル生成(`addTable`内部の書き込み)が同じ範囲へ二重に値を書き込んでいたため、セル直接書き込みを削除し、テーブル生成側の書き込みに一本化した。
- マスタ参照(`lookups`)のキー収集で、同じキーが多数の行で共有される場合に重複したままカーソルAPIへ問い合わせていたため、重複除去してから問い合わせるようにした。
- `sanitizeExcelTableName`(`src/shared/excel.ts`)が半角英数字以外(日本語などのUnicode文字)を全て `_` に置き換えていたため、`tableName` に日本語を含めると意図した名前(例: `見通しTable`)にならず、実際には別の名前(例: `Table`)になっていた。Excelのテーブル名は日本語などのUnicode文字を使えるため、`\p{L}`(Unicode文字)・`\p{N}`(数字)・`_` を残すように修正した。
- 初期Excelテンプレート作成時は取得件数が常に0件のため、ヘッダー行のみ・データ行0件のExcelテーブルを生成していた。データ行0件のテーブルはExcelの仕様上不正になり、開いたときに修復ダイアログが出る原因になっていたため、データ行が0件の場合は空欄1行を補って最低1データ行を確保するようにした。

## 実装済みの拡張

相談済みだった機能は、以下のPhase 1からPhase 6まで実装済みである。次の追加開発は、実運用で確認した課題をもとに別途決める。

### 次の実装順

#### Phase 1: JSON手書き削減

完了済み:

- 複数取得元アプリブロックを追加、削除、複製できるUI
- 既存JSONから複数ブロックを復元できるUI
- 各ブロックごとのフィールド行追加、削除

#### Phase 2: フィールド定義取り込み

完了済み:

- kintoneのフォームフィールド定義APIからフィールド一覧を取得
- kintoneフィールド型から `text` / `number` / `date` / `datetime` を推定
- チェックボックスで削除フィールドへ移動する行を選択
- 削除対象の全選択、全解除、選択行の削除フィールド移動
- 上下移動でExcel列順を変更
- 出力フィールドと削除フィールドを左右表示し、削除フィールドから戻せる

#### Phase 3: 抽出条件の汎用化

完了済み:

- `filters` 配列で複数の抽出条件を設定
- 対象店舗、基準日から計算する期間、基準日、固定値を条件値として選択
- 日付条件ごとに月初から基準日、1月1日から基準日などを選択
- 固定値の文字・日付型と数値型を選択
- 日付条件がないアプリと条件なし全件取得に対応
- `sorts` 配列で日付条件とは独立した複数の並び順を設定

```json
{
  "filters": [
    { "field": "店舗ID", "operator": "=", "valueFrom": "store", "valueType": "text" },
    { "field": "日付", "operator": "between", "valueFrom": "dateRange", "valueType": "text", "dateRule": "monthStartToBaseDate" }
  ],
  "sorts": [
    { "field": "店舗ID", "order": "asc" },
    { "field": "日付", "order": "asc" }
  ]
}
```

#### Phase 4: マスタ参照

店舗IDだけ持っている実績データと、店舗ID、店舗名、営業部などを持つ店舗マスタを組み合わせる。

完了済み:

- `lookups` 設定に基づき、取得元データのキーからマスタアプリを参照して行データへ補完する
- マスタ参照用のキーは、Excel出力列に含めない場合でも内部取得できる

想定JSON:

```json
{
  "lookups": [
    {
      "sourceField": "店舗ID",
      "masterAppId": "201",
      "masterKeyField": "店舗ID",
      "masterFields": [
        { "code": "店舗名", "label": "店舗名" },
        { "code": "営業部", "label": "営業部" }
      ]
    }
  ]
}
```

#### Phase 5: 出力アプリの初期値と履歴

完了済み:

- 基準日は常に昨日
- 帳票出力時は保存済み・手入力の基準日ではなく、実行日の前日をリアルタイムに優先
- 明示的に基準日を昨日へ更新するボタン
- 期間開始/期間終了は基準日から自動計算
- 例: `2026-07-30` に開いた場合、基準日 `2026-07-29`、期間開始 `2026-07-01`、期間終了 `2026-07-29`
- 例: `2026-08-01` に開いた場合、基準日 `2026-07-31`、期間開始 `2026-07-01`、期間終了 `2026-07-31`
- 出力条件をExcelの設定シートに詳しく出す
- 出力時に確定した基準日をkintoneレコード側にも残す
- 出力日時、出力者、出力ファイル名、ステータスをkintoneレコード側にも残す
- 出力条件メモをkintoneレコード側にも残す

#### Phase 6: 検証、エラー表示、進捗

完了済み:

- 帳票出力前に取得元アプリ、マスタアプリ、フィールド、抽出条件、並び順の存在を検証する
- テンプレート管理アプリの詳細画面で、完成版テンプレート添付と取得元アプリ設定を検証する
- 帳票出力アプリの詳細画面で、実データ取得前にテンプレート連携と取得元設定を検証する
- 検証エラーや帳票出力エラーは詳細ダイアログで表示する
- 取得元アプリごとの取得件数とマスタ参照件数をステータス表示する
- テンプレート管理アプリ上で完成版テンプレート添付を保存補助する
- 完成版テンプレート添付が未登録または複数登録の場合は帳票出力を中断する

## 禁止事項

- 実運用のAPIトークンを保存しない
- パスワードを保存しない
- 個人情報や実データをサンプルとして保存しない
- 集計シートの数式をTypeScript側で再現しない
- 生成済みExcelやzipをソース管理対象にしない
- 関係ないリファクタリングを混ぜない
